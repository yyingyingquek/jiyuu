import React, {useEffect} from 'react';

const OrderPage = () => {

    const getOrder = async () => {
        
    }

    useEffect(()=>{})

    return (
        <div>
            order page
        </div>
    );
};

export default OrderPage;